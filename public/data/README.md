These are copies of real, verified public datasets — served here so the client-side
Graphing Explorer can `fetch()` them directly (browsers can't reach files outside
`frontend/public/`).

Canonical source + provenance: `data/public/naep-math-trends/SOURCE.md` and
`data/public/pisa-math-scores-owid/SOURCE.md` at the repo root. Kept in sync by hand —
same pattern already used for the mock dataset fixtures in this project.
